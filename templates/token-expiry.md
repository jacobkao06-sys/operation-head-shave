Subject: OPERATION HEAD SHAVE — Instagram token expires in {{days_until_token_expiry}} days

{{reason}}

  EXPIRES ........... {{token_expires_local}}

An expired token CANNOT be refreshed. Re-run the OAuth flow before that date:
{{public_url}}/api/auth/instagram/start
